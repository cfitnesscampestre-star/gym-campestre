{
  "rules": {
    "socios": {
      ".read": true,
      ".write": true,
      "$code": {
        ".validate": "newData.hasChildren(['nombre','objetivo','codigo']) || newData.hasChildren(['nombre','objetivo'])"
      }
    },
    "entrenadores": {
      ".read": true,
      ".write": true
    }
  }
}
