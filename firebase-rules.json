{
  "rules": {
    "socios": {
      ".read": true,
      "$code": {
        ".write": "newData.exists()",
        ".validate": "newData.hasChildren(['nombre','objetivo'])"
      }
    },
    "entrenadores": {
      ".read": true,
      "$id": {
        ".write": true
      }
    },
    "config": {
      ".read": true,
      "$k": {
        ".write": "newData.exists()"
      }
    }
  }
}